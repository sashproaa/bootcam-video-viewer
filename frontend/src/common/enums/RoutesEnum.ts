export enum Routes {
  catalog = '/catalog',
  video = '/video',
  login = '/login',
  registration = '/registration',
  subscription = '/subscription',
  profile = '/profile',
  payment = '/payment',
}
